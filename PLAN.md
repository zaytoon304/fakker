# فكّر (Fakker) — منصة الألعاب الذهنية

منصة ألعاب تعليمية واحدة تجمع عدة ألعاب ذهنية، بخمس مستويات دراسية، كل مستوى يشوف نسخة من الألعاب تناسب عمره وقدرته.

## المستويات الخمسة
1. 🟢 المستوى الأول — صف أول ابتدائي
2. 🔵 المستوى الثاني — صف ثاني وثالث ابتدائي
3. 🟣 المستوى الثالث — صف رابع/خامس/سادس ابتدائي
4. 🟠 المستوى الرابع — متوسط
5. 🔴 المستوى الخامس — ثانوي

## الألعاب
| اللعبة | الفكرة | كيف تصعب مع المستوى |
|---|---|---|
| 🧠 الذاكرة | قلب البطاقات وطابق الأزواج | عدد البطاقات (٦ → ١٢ → ١٦ → ٢٤ → ٣٦) + المحتوى (صور → كلمات → مفاهيم/رموز) |
| ⭕ إكس أو المطورة | تيك تاك تو ضد الحاسوب أو صديق | لوح ٣×٣ بذكاء بسيط → لوح ٤×٤/٥×٥ واستراتيجية "الوصل ٤" → إكس أو ألتيميت (٩ ألواح داخل لوح) للثانوي |
| 🔢 السودوكو التصاعدي | ترتيب أرقام بلا تكرار | ٤×٤ → ٦×٦ → ٩×٩ سهل → ٩×٩ صعب |
| ➕ عبقرينو-رياضيات | حل مسائل حسابية بسرعة | جمع/طرح ضمن ١٠ → جدول الضرب/القسمة → كسور/نسب → معادلات بسيطة → مسائل منطقية رياضية |
| 🧩 ألغاز التفكير العليا | أنماط، تسلسل، استنتاج، تصنيف | ألغاز بصرية بسيطة → تسلسل أرقام/أشكال → استنتاج منطقي → ألغاز متعددة الخطوات |

## القرارات التقنية
- HTML/CSS/JS عادي بدون أي أدوات بناء (مثل باقي مشاريعك — يفتح بالمتصفح مباشرة)
- Firebase Realtime Database لحفظ التقدم (نفس أسلوب عبقرينو) — بروفايل بسيط بالاسم فقط، بدون كلمات مرور
- كل لعبة ملف HTML منفصل داخل `games/` تفتح من الصفحة الرئيسية
- ملفات مشتركة بـ `shared/` (الإعدادات، حفظ التقدم، التنسيق العام)
- تصميم متجاوب (جوال/تابلت/كمبيوتر) وعربي RTL بالكامل
- بدون حسابات معلم أو متجر مكافآت — تركيز كامل على الألعاب فقط

## هيكل الملفات
```
fakker/
├── index.html          ← الصفحة الرئيسية (اختيار المستوى ثم الألعاب)
├── PLAN.md
├── CLAUDE.md
├── games/
│   ├── memory.html      ← لعبة الذاكرة
│   ├── xo.html          ← إكس أو المطورة
│   ├── sudoku.html      ← السودوكو التصاعدي
│   ├── math.html        ← عبقرينو-رياضيات
│   └── logic.html       ← ألغاز التفكير العليا
└── shared/
    ├── style.css        ← تنسيق موحد لكل الألعاب
    ├── firebase-config.js
    └── progress.js      ← حفظ/قراءة التقدم والنجوم
```

## خطوات البناء (خطوة بخطوة، اختبار بعد كل خطوة)
- [x] ١. إنشاء المجلد + PLAN.md
- [x] ٢. الصفحة الرئيسية index.html (اختيار المستوى + عرض بطاقات الألعاب) — تم واختُبرت بالمتصفح ✅
- [x] ٣. shared/style.css — تنسيق موحد يُستخدم بكل الصفحات
- [x] ٤. shared/firebase-config.js + shared/progress.js — بروفايل الطالب وحفظ التقدم (يعمل محلياً بدون Firebase حتى تُضاف مفاتيح مشروع حقيقي)
- [x] ٥. games/memory.html — لعبة الذاكرة، بمحتوى مختلف تماماً لكل مستوى (فواكه → علاقات → معادلات → مفردات → عناصر كيميائية)، اختُبرت بالمتصفح ✅
- [x] ٦. games/xo.html — إكس أو تتصاعد فعلياً: قطة ضد كلب (٣×٣ سهلة) → كلاسيكية بذكاء لا يُقهر (Minimax) → شبكة ٤×٤ → شبكة ٥×٥ (٤ متتالية) → إكس أو ألتيميت (٩ ألواح) للثانوي، + خيار لعب مع صديق. اختُبرت بالمتصفح ✅
- [x] ٧. games/sudoku.html — السودوكو التصاعدي (٤×٤ → ٦×٦ → ٩×٩ سهل/متوسط/صعب)، مولّد ألغاز بحل وحيد مضمون (بحث بالرجوع + رصد الاحتمالات)، تحقق تعارضات حي، نظام تلميحات. اختُبرت بالمتصفح ✅
- [x] ٨. games/math.html — عبقرينو-رياضيات: أسئلة سريعة متعددة الخيارات + مؤقت + ستريك، محتوى مختلف لكل مستوى (جمع بسيط → ضرب/قسمة → كسور ونسب → جبر وجذور). اختُبرت بالمتصفح ✅
- [x] ٩. games/logic.html — ألغاز تفكير عليا: أنماط بصرية → تصنيف → متتاليات → تناظرات لفظية → استنتاج منطقي. اختُبرت بالمتصفح ✅
- [x] ١٠. عرض النجوم/التقدم — مُنجز فعلياً عبر: نجوم كل لعبة على بطاقتها، إجمالي نجوم المستوى، وإجمالي نجوم الطالب بشارة الرئيسية (لا حاجة لشاشة منفصلة)
- [ ] ١١. لمسات أخيرة: اختبار الجوال/التابلت، تهيئة git محلي، ثم قرار محمد بخصوص رفع GitHub (عام/خاص)

## ملاحظات
- كل لعبة تُفتح بصفحتها الخاصة عشان يسهل نبنيها ونختبرها لحالها بدون ما نكسر الباقي
- التقدم يُحفظ محلياً (localStorage) فوراً + يتزامن مع Firebase بالخلفية (نفس فكرة عبقرينو)

## تحديث مهم (2026-09-05): دمج الألعاب الحقيقية بدل النسخ المبسّطة
محمد وضّح إن "عبقرينو" لازم تكون اللعبة الحقيقية المنشورة (مو نسخة أبنيها من الصفر)، وعنده ألعاب أخرى جاهزة منشورة على GitHub Pages من مدارس الأرقم يبي يجمعها بنفس المنصة.

- [x] استبدال games/math.html (نسخة مبسّطة كنت بنيتها) بربط **عبقرينو الحقيقية**: `https://zaytoon304.github.io/abaqreeno/`
- [x] إضافة **رحلة الأرقام**: `https://zaytoon304.github.io/number-journey/`
- [x] إضافة **بازل رياضي**: `https://zaytoon304.github.io/math-puzzle/puzzle.html`
- [x] بناء `games/embed.html` — صفحة عامة تفتح أي لعبة خارجية داخل إطار (iframe) بهوية فكّر (شريط علوي + عنوان + رجوع + فتح بنافذة جديدة)، تأخذ الرابط والعنوان عبر باراميترات الرابط
- [x] تعديل index.html: الألعاب الخارجية تعرض شارة "🌐 من مدارس الأرقم" بدل النجوم (لأن نظام تقييمها ونقاطها منفصل بالكامل عن فكّر)
- [x] اختبار الاثنين بالمتصفح فعلياً — يحمّلون صح جوّا الإطار (يحتاجون ٥-٨ ثواني تحميل بسبب Firebase)
- [x] بحثت بنفسي بحساب GitHub (zaytoon304) عن "اللعبة الرابعة" ولقيت لعبتين رياضيات مطابقتين للوصف، أضفتهما الاثنتين: **شبكة الحساب** (arqam-math-grid) و**الحساب الذهني** (mental-math-flash) — اختُبرتا بالمتصفح ✅

## لعبة جديدة (2026-09-05): فصحون — لغة عربية بهوية بصرية خاصة
محمد طلب لعبة عربية شاملة (حروف/كلمات/جمل/تناظرات/قدرات) بمستوى "عالمي" لا مجرد قالب أسئلة عادي.

- [x] `games/fasihoon.html` — 5 مستويات مطابقة تماماً لمواصفاته:
  - أول ابتدائي: حرف البداية، الحرف الناقص (بداية/وسط/آخر)، ترتيب حروف كلمة ثلاثية
  - ثاني وثالث: ترتيب كلمات أطول من ٣ حروف، كلمات المدّ
  - رابع-خامس-سادس: ترتيب كلمات لجملة، ترتيب جمل لقصة
  - متوسط: تناظر لفظي، مفردة شاذة، أسماء صغار الحيوانات، صفات الألوان
  - ثانوي: قدرات مختلطة (تناظر معقد + منطق جمل + رياضيات)
- [x] هوية بصرية خاصة: شريط ذهبي بخط Reem Kufi + Amiri (بدل التصميم النمطي)، شخصية "الأستاذ فصيح" 🦉 تتفاعل بالمزاج (سعيد/تشجيع) وتعطي تلميحات، بطاقات ذهبية لعرض الحروف/الكلمات المبعثرة بصرياً بدل نص عادي
- [x] أُضيفت لقائمة الألعاب في shared/progress.js، اختُبرت بالمتصفح ✅

## رفع مستوى الصفحة الرئيسية (2026-09-05)
بعد فصحون، رُفع مستوى التصميم بالصفحة الرئيسية نفسها لنفس المعيار ("أكمل وأبدع"):
- [x] خلفية متدرجة ناعمة بعمق بصري بكل صفحات المنصة (shared/style.css)
- [x] ظلال طبقات أكثر واقعية للبطاقات (بدل ظل مسطح واحد)
- [x] بطاقات المستويات: شارة أيقونة زجاجية (🎈📚🔬💡🎓) + رسمة خلفية كبيرة شفافة + وسم "المرحلة رقم" + تدرّج لوني حقيقي لكل مستوى (كان فيه خطأ فني أول مرة: محاولة دمج شفافية hex مع متغيّر CSS var() ما تصلح — أُصلح عبر تدرّجات محددة بـ data-level)
- [x] بطاقات الألعاب: شارة أيقونة بتدرّج + زر لعب بتدرّج وظل، تأثير ارتفاع خفيف عند المرور
- [x] ترحيب شخصي متغيّر باسم الطالب بدل نص ثابت
- [x] اختُبر بالمتصفح ✅

## بحث عالمي + ميزات جديدة (2026-09-05): "أكمل وأبدع"
محمد طلب البحث بمواقع الألعاب التعليمية العالمية وجلب ما هو جديد للمنصة باحترافية. بحثت فعلياً (Duolingo، Prodigy، Kahoot، Blooket) ولقيت آليتين ناقصتين تماماً بفكّر:

**١. تحدي اليوم (`games/daily.html`)** — بأسلوب Wordle/Duolingo:
- [x] سؤال واحد يومي ثابت لكل الطلاب (يُختار بالاعتماد على تاريخ اليوم كبذرة)، بدون مؤقت (طابع هادئ متعمّد يختلف عن الألعاب الأخرى)
- [x] ستريك أيام متتالية منفصل تماماً عن نجوم الألعاب (`Fakker.Daily` بـ shared/progress.js)
- [x] "حماية ستريك" 🧊 (Duolingo streak freeze) — تُمنح تلقائياً كل أسبوع متواصل، تغطي يوم فائت بدون كسر السلسلة
- [x] شريط أسبوعي بصري (✓/·) يعرض آخر 7 أيام
- [x] بانر بارز بالصفحة الرئيسية فوق شبكة المستويات، يعرض الستريك الحالي ويتحدّث تلقائياً
- [x] هوية بصرية خاصة (شمس متوهجة + سماء متدرّجة) — اختُبر بالمتصفح بالكامل (إجابة صحيحة → تحديث الستريك → حالة "محلول من قبل" عند إعادة الدخول) ✅

**٢. قدرات مساعدة Power-ups** — بأسلوب Kahoot/Blooket:
- [x] "🎯 50:50" بلعبتي فصحون وألغاز التفكير: يحذف إجابتين خاطئتين، مرتين لكل جولة، يتجدد بجولة جديدة
- [x] اختُبر بالمتصفح ✅

## وضع لعب جديد كلياً (2026-09-05): ساحة الأبطال — معركة RPG
محمد طلب استكمال احترافي عالمي إبداعي "ولا تكن تقليدي". بدل تكرار قالب الأسئلة، بُني وضع لعب مختلف جذرياً بأسلوب Prodigy Math:

- [x] `games/battle.html` — معركة ضد وحش: البطل (يستخدم شخصية الطالب المختارة) ضد وحش مختلف بكل مستوى (🐲 تنين صغير خجول ← 👹 غول المكتبة ← 🐙 أخطبوط الألغاز ← 🗿 حارس التماثيل ← 🐉 تنين الحكمة العظيم)
- [x] كل إجابة صحيحة = ضربة تُنقص شريط حياة الوحش (نبضات/اهتزاز + نص طائر "⚔️ ضربة!")؛ كل إجابة خاطئة = "رد الوحش" يُنقص قلب من قلوب البطل الثلاثة (بدون لغة سلبية — "الوحش قوي هالمرة" لا "خسرت")
- [x] محتوى ثقافة عامة متنوع جديد كلياً (لا يوجد بأي لعبة سابقة): رياضيات + علوم + جغرافيا + لغة، متصاعد الصعوبة بكل مستوى
- [x] هوية بصرية فريدة: ساحة كونية مظلمة بنجوم متلألئة، شريط حياة مقسّم، حركات ضرب واهتزاز
- [x] النجوم مبنية على عدد القلوب المتبقية عند الانتصار (دايماً ★ واحدة أقل تقدير حتى عند الهزيمة — لا صفر أبداً)
- [x] أُضيفت لقائمة الألعاب، اختُبرت بالمتصفح بالكامل (ضربة صحيحة → نقص حياة الوحش، إجابة خاطئة → نقص قلب البطل) ✅

**أفكار مستقبلية إضافية** (لم تُنفَّذ): فتح شخصيات/إطارات صورة جديدة بالنجوم المتراكمة، أسلحة/دروع يفتحها البطل بعد كل انتصار.

## نظام النقاط والمكافآت الكامل (2026-09-05)
محمد أعاد إرسال البرومبت الاحترافي الأصلي (نظام تشجيع + ستريك + مضاعف + محفظة + متجر + استبدال + لوحة معلم) بعد ما لاحظ إنه ناقص من الألعاب. نُفّذ بالكامل بأسلوب Frontend + طبقة Service جاهزة لقاعدة بيانات لاحقاً (بدون كسر أي لعبة موجودة):

- [x] `shared/economy-config.js` — كل قيم النقاط والمضاعفات بملف واحد مركزي
- [x] `shared/economy.js` — `Fakker.Wallet` (رصيد/إجمالي/سجل)، `Fakker.Economy` (حساب المضاعف والنقاط)، `Fakker.Celebrate` (نافذة احتفال عائمة)، `Fakker.Rewards`، `Fakker.Redemptions` (Pending/Approved/Rejected/Redeemed + حجز نقاط فوري + استرجاع تلقائي عند الرفض + منع تكرار القبول/الرفض)، `Fakker.TeacherAuth` (بوابة PIN)
- [x] `shared/progress.js` — أُضيف `Fakker.Settings` (كتم صوت/تقليل حركة) و`Fakker.mountSettingsToggle()` (⚙️ بكل صفحة)
- [x] `store.html` — متجر المكافآت (تصنيفات، تأكيد قبل الاستبدال، تتبع "طلباتي")
- [x] `teacher.html` — لوحة معلم كاملة خلف PIN (افتراضي 2026، قابل للتغيير): الموافقات، إدارة المكافآت (CRUD)، تعديل قيم النقاط/المضاعفات، سجل نقاط الجهاز، تنبيه أمان صريح
- [x] `tests.html` — 30 اختبار آلي (نقاط أساسية، مضاعفات، منع رصيد سالب، منع خصم مزدوج، استرجاع عند الرفض، Idempotency، سلامة الألعاب القديمة) — **30/30 ناجح** ✅، معزولة تماماً عن بيانات المستخدم الحقيقية
- [x] رُبطت بألعاب: منطق، فصحون، تحدي اليوم، ساحة الأبطال (نقاط + مضاعف + نافذة احتفال + رصيد بالشريط العلوي) — تُرك الحساب من نظام النقاط خارج الذاكرة/إكس أو/سودوكو عمداً لأنها ليست "أسئلة وأجوبة" (لا تملك مفهوم "إجابة صحيحة")، تبقى بنظام النجوم فقط
- [x] اختُبر يدوياً بالمتصفح بالكامل: شراء رقمي فوري، شراء يحتاج موافقة، قبول ورفض من لوحة المعلم، تحديث المخزون، تنظيف بيانات الاختبار من localStorage الحقيقي بعدها

**⚠️ قيد معماري صريح (لازم يعرفه محمد):** بدون Firebase حقيقي، كل بيانات المحفظة/الطلبات محفوظة محلياً على نفس الجهاز/المتصفح فقط — لوحة المعلم ما تشوف طلبات كل الطلاب من كل الأجهزة إلا بعد تفعيل Firebase (قرار معلّق من قبل).

- [x] ربط مكافأة "إكمال النشاط" (+50) بألعاب الذاكرة وإكس أو والسودوكو أيضاً (كانت هذي الثلاث بدون أي نقاط)، وربط زر تلميح السودوكو بخصم نقطة تلميح — اختُبر بالمتصفح لكل الثلاث ✅
- [x] رُفع المشروع بالكامل على GitHub: `https://github.com/zaytoon304/fakker`
- [x] المستودع صار **عام** + فُعِّل **GitHub Pages** بناءً على طلب محمد (المنصة راح تُعرض على سبورات المدرسة الذكية، تحتاج رابط دائم يفتح من أي جهاز بلا سيرفر محلي): **الرابط الدائم: https://zaytoon304.github.io/fakker/**

## بوابة "حصة الانتظار" (2026-09-06)
محمد وضّح سيناريو الاستخدام الحقيقي: بورد ذكي واحد بالفصل، يفتح صفحة فكّر وقت انتظار المعلم (بدل ما الطلاب يشاغبون أو يفتحون يوتيوب)، ويتناوب عليه فصول/حصص مختلفة طول اليوم.

- [x] شاشة **"⏳ حصة انتظار"** تظهر أول شي قبل أي محتوى: تطلب اسم الفصل + اختيار الحصة (١-٨) + ملاحظة اختيارية، ولازم تُملأ قبل ما تفتح أي لعبة
- [x] تُسجَّل مرة وحدة لكل جلسة متصفح (sessionStorage) — ما يُعاد سؤالها عند التنقل بين الصفحات بنفس الجلسة، لكن تُطلب من جديد إذا انسكر المتصفح/التبويب (يعني تلقائياً تتجدد كل ما يفتحون الصفحة من جديد بحصة انتظار تالية)
- [x] شارة بأعلى الصفحة تعرض الفصل والحصة الحالية، بالضغط عليها تقدر "تنهي الحصة" يدوياً (يفيد لو نفس التبويب يتناوب عليه أكثر من فصل بدون إغلاقه)
- [x] **حماية فعلية**: كل صفحات الألعاب (بما فيها الروابط المباشرة الخارجية) تتحقق من تسجيل الحصة وتُرجع لبوابة الدخول لو حاول أحد يفتح لعبة برابط مباشر متجاوزاً البوابة
- [x] تبويب جديد بلوحة المعلم **"سجل حصص الانتظار"** يعرض كل تسجيلات الدخول (فصل/حصة/وقت) لهذا الجهاز
- [x] اختُبر بالكامل بالمتصفح: البوابة تظهر أول شي، التسجيل ينجح وينتقل للمنصة، الدخول المباشر للعبة بدون تسجيل يُرفض ويرجّع للبوابة، السجل يظهر بلوحة المعلم ✅
- [x] رُفع كل التحديث على GitHub (المستودع العام + Pages)

## هوية بصرية مختلفة لكل لعبة (2026-09-05)
بعد ساحة الأبطال، اكتملت الفكرة على باقي الألعاب القديمة (كانت لسه بالقالب الأرجواني النمطي):
- [x] **لعبة الذاكرة** → "طاولة بطاقات" فاخرة: سطح غامق بنقشة ماسية، ظهر البطاقة بلون المستوى + أيقونة الثيم، توهج ذهبي عند المطابقة
- [x] **إكس أو** → "شاشة أركيد": إطار غامق نيون بنفسجي، الألواح الفرعية (ألتيميت) بنفس الطابع، توهج ذهبي نابض عند الفوز
- [x] **سودوكو** → "لوح مخطط هندسي": ورق مربّعات أزرق داكن، إطار كمبس 📐، أرقام بخط Mono تقني، تحديد الخلية بإطار كهرماني
- كل لعبة الآن عالمها البصري الخاص (طاولة بطاقات / أركيد / مخطط هندسي / ساحة كونية / مخطوطة ذهبية / سماء مشمسة) بدل تكرار نفس القالب — اختُبرت الثلاث بالمتصفح ✅
